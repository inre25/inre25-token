# Metadata

Final token metadata JSON is not included yet.

Confirmed decimals: 9.

Published GitHub repository: https://github.com/inre25/inre25-token

Pending before `token.json`:

- final metadata JSON schema/format for target catalog
- final external explorer/catalog requirements

Do not create or publish `token.json` until these values and the required target format are confirmed.
